Function:hero(bullets,dragon){
    if(bullets/2>=dragons) // Since each dragon takes 2 bullets we need to make sure he has enough
    return = "true";
}else{
    (bullets/2<dragons)
    return = "false";
}
